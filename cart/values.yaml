namespace: roboshop
deployment:
  replicaCount: 1
  imageRepo: joindevops/cart
  imageVersion: v1
  imagePullPolicy: Always
service:
  servicePort: 8080
  targetPort: 8080
configMap:
  REDIS_HOST: redis
  CATALOGUE_HOST: catalogue
  CATALOGUE_PORT: "8080"


# helm install cart . -n roboshop -f values-dev.yaml
# helm upgrade cart . -n roboshop -f values-dev.yaml
# helm uninstall cart -n roboshop
